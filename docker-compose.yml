version: '3.8'
services:
  tableau_data_storytelling_sprint:
    image: python:3.11-slim
    working_dir: /app
    volumes:
      - .:/app
    command: bash -c "pip install -r requirements.txt && python src/data_generator.py && python src/core_engine.py && pytest tests/"